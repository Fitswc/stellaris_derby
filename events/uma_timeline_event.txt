namespace = uma_timeline


event = {
	id = uma_timeline.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_playable_country = {
			limit = {
				is_country_type = default
				has_authority = auth_trainingcenter
			}

			country_event = {
				id = uma_timeline.2
			}
		}
	}
}


country_event = {
	id = uma_timeline.2
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		switch = {
			trigger = has_origin
			origin_uma_below_threegodness = {
				add_timeline_event = {
					type = timeline_origin
					override_tooltip = "origin_uma_below_threegodness_desc"
					override_text = { "text:origin_uma_below_threegodness" }
					override_texture = { "background:GFX_origin_below_threegodness" }
					override_id = timeline_origin_uma_below_threegodness
				}
			}

			origin_uma_candleoff = {
				add_timeline_event = {
					type = timeline_origin
					override_tooltip = "origin_uma_candleoff_desc"
					override_text = { "text:origin_uma_candleoff" }
					override_texture = { "background:GFX_origin_uma_candleoff" }
					override_id = timeline_origin_uma_candleoff
				}
			}

			origin_uma_business_MAN = {
				add_timeline_event = {
					type = timeline_origin
					override_tooltip = "origin_uma_business_MAN_desc"
					override_text = { "text:origin_uma_business_MAN" }
					override_texture = { "background:GFX_origin_uma_business_MAN" }
					override_id = timeline_origin_business_MAN
				}
			}
		}
	}
}